<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Andronix.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Andronix.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Activated="Window_Activated"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <WebView2 x:Name="_responseView" Grid.Column="0" Grid.Row="0" ></WebView2>
        <Grid Grid.Column="0" Grid.Row="1" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="_promptText" AcceptsReturn="True" 
                     Grid.Column="0" Grid.Row="0" 
                     PreviewKeyDown="PromptText_PreviewKeyDown" />
            <Button x:Name="_goButton" Grid.Column="1" Grid.Row="0" Click="GoButton_Click">Go ▷</Button>
        </Grid>
    </Grid>
    
</Window>
